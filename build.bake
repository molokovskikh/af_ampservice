import file from Web.bake
import file from Db.bake

Global(
	Project : "AmpService",
	HumanReadableName : "Служба для Амп"
)

task @Build, [@WebBuild]

task @default, [@Build]

task @deploy, [@DeployPipeline]
